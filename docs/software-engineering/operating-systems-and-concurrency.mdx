---
title: Operating Systems & Concurrency
description: Topic landing page cho execution, shared-state correctness, waiting, memory và CPU scheduling.
slug: /software-engineering/operating-systems-and-concurrency
---

import {KnowledgeMap, LandingCard, LandingCardGrid} from '@site/src/components/Learning';

# Operating Systems & Concurrency

Operating Systems & Concurrency explains **where work executes, what it shares, what it waits for and which finite resource limits throughput or correctness**.

## Topic Overview

<KnowledgeMap
  label="Operating Systems and Concurrency topic map"
  levels={[
    [{title: 'Execution Boundary', description: 'Start with processes, threads and concurrent work to locate where execution happens.'}],
    [{title: 'Shared State', description: 'Reason about interleaving, synchronization, race conditions and correctness.'}],
    [{title: 'Waiting Model', description: 'Distinguish blocking, non-blocking and async I/O behavior when work waits.'}],
    [{title: 'Memory', description: 'Inspect stack, heap, virtual memory, leaks and out-of-memory pressure.'}],
    [{title: 'CPU Scheduling', description: 'Understand runnable work, context switching and worker sizing under finite CPU.'}],
  ]}
/>

## Lessons

<LandingCardGrid>
  <LandingCard eyebrow="01" title="Process, Thread & Concurrency" description="Execution boundaries, concurrency vs parallelism, interleaving and race conditions." to="./process-thread-and-concurrency" />
  <LandingCard eyebrow="02" title="Synchronization & Deadlocks" description="Critical sections, mutexes, lock contention, deadlock and lock ordering." to="./synchronization-and-deadlocks" />
  <LandingCard eyebrow="03" title="Async I/O & Event Loop" description="Blocking vs non-blocking waiting, event loops, async/await and bounded concurrency." to="./async-io-and-event-loop" />
  <LandingCard eyebrow="04" title="Memory Management" description="Stack, heap, virtual memory, leaks, stack overflow and OOM." to="./memory-management" />
  <LandingCard eyebrow="05" title="CPU Scheduling & Context Switching" description="Runnable work, scheduler behavior, context-switch cost and worker sizing." to="./cpu-scheduling-and-context-switching" />
</LandingCardGrid>

## Recommended Order

Go from execution boundaries → synchronization → waiting model → memory → CPU scheduling. In production debugging, jump directly to the lens matching the observed bottleneck.
