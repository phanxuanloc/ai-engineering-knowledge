---
title: Tên technical concept
description: Mô tả bằng một câu để phân biệt rõ phạm vi của note.
tags: [primary-topic, related-topic]
draft: true
---

import {Comparison, ConceptCard, ConceptGrid, Principle, TLDR} from '@site/src/components/Learning';
// Import only when used:
// import {ExcalidrawDiagram, ReactFlowDiagram} from '@site/src/components/Diagrams';

# Tên technical concept

<!--
Ngôn ngữ chính là tiếng Việt; giữ English technical terms. Trước khi author hoặc
refactor note, bắt buộc chạy docs-visual-density để audit từng prose block, bullet
cluster và fenced text block; sau đó dùng docs-diagram để route renderer.

Visual-first rule: nếu nội dung chủ yếu biểu diễn flow/lifecycle, topology/hierarchy,
state transition, causality, comparison hoặc quantitative change và shared renderer
có thể biểu diễn đúng semantics, phải dùng visual. Không dùng ASCII/pseudo-diagram
thay cho ReactFlowDiagram, FlowExplainer, Excalidraw, Comparison hoặc chart trừ khi
literal terminal/text output là chính nội dung cần dạy. Sau khoảng hai substantial
conceptual blocks liên tiếp không có visual anchor, re-run visual-density audit.

Giữ prose cho exact definition, caveat, invariant, code/config/API example và phần
đã rõ trong 2–4 dòng. Visual phải dạy nhanh hơn text, không được chỉ trang trí.
-->

## TL;DR

<TLDR>

Tóm tắt takeaway quan trọng nhất trong hai hoặc ba câu. Nêu concept là gì và vì sao nó quan trọng.

</TLDR>

## Mental Model

Giải thích mental model đơn giản nhất. Nếu mental model có structure/behavior/spatial relationship, visual là default; prose-only cần lý do cụ thể vì sao visual không giúp hiểu nhanh hơn.

## Core Concepts

- **Concept:** Định nghĩa đủ ngữ cảnh và giải thích vì sao quan trọng.
- **Related concept:** Tóm tắt ngắn rồi link canonical learning note khi thực sự hữu ích.
- Không tạo note cho concept chưa học chỉ để có link.

## Example

Đưa ra một ví dụ nhỏ và cụ thể. Ưu tiên executable code/sample data cho exact mechanics; ưu tiên flow/diagram cho behavior hoặc structure.

```ts
// Minimal exact code example when code itself is the lesson.
```

## When to Use

- Tình huống phù hợp.
- Signal cho thấy nên chọn approach.
- Trade-off làm thay đổi quyết định.

## Common Mistakes

- **Mistake:** Giải thích vì sao lỗi xảy ra và cách tránh/phát hiện.

## Related Knowledge

Chỉ link tới learning note thực sự tồn tại và có giá trị bổ sung.

## Self-test

1. Recall question?
2. Mental-model question?
3. Application scenario?
4. Trade-off question?

<details>
<summary>Answers</summary>

1. Câu trả lời.
2. Câu trả lời.
3. Câu trả lời.
4. Câu trả lời.

</details>
