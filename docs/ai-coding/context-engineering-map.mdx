---
title: Context Engineering Map
description: Canonical map để recall Context Engineering, theo dõi lifecycle và đi tới đúng deep dive mà không phải đọc một bài monolith.
tags: [context-engineering, learning-map, ai-agent]
sidebar_position: 2
---

import {ReactFlowDiagram} from '@site/src/components/Diagrams';

# Context Engineering Map

Trang này là **entry point + canonical lifecycle** của Context Engineering. Nó không lặp lại toàn bộ chi tiết; mục tiêu là giúp recall mental model trong khoảng 30 giây rồi đi tới đúng deep dive.

## One Mental Model

> **Context Window là cái bàn. Context là những thứ đặt trên bàn. Context Engineering là quyết định đúng thứ cần đặt lên bàn cho current decision.**

Nguyên tắc xuyên suốt: **Relevant context > More context**. Window lớn hơn tăng capacity nhưng không tự làm retrieval, selection, quality, freshness hay trust tốt hơn.

## Canonical Lifecycle

<ReactFlowDiagram
  ariaLabel="Canonical Context Engineering lifecycle"
  direction="TB"
  kind="lifecycle"
  layout="primary-path"
  primaryPath={[
    'sources',
    'retrieve',
    'candidates',
    'select',
    'optimize',
    'assemble',
    'window',
    'llm',
    'observe',
    'working-state',
    'evict',
  ]}
  nodes={[
    {id: 'sources', eyebrow: 'Available', label: 'Sources', detail: 'Code · Memory · Docs · Tools · State', role: 'source', size: 'wide'},
    {id: 'retrieve', eyebrow: 'Find', label: 'Retrieve', detail: 'Tạo candidate information', role: 'process', size: 'standard'},
    {id: 'candidates', eyebrow: 'Pool', label: 'Candidates', detail: 'Có thể hữu ích', role: 'state', size: 'standard'},
    {id: 'select', eyebrow: 'Decide', label: 'Select', detail: 'Useful cho current decision?', role: 'process', size: 'standard'},
    {id: 'optimize', eyebrow: 'Prepare', label: 'Quality · Budget · Compress', detail: 'Đủ, đúng, ít noise, vừa capacity', role: 'process', size: 'wide'},
    {id: 'assemble', eyebrow: 'Package', label: 'Assemble', detail: 'Authority · Task · State · Evidence', role: 'process', size: 'wide'},
    {id: 'window', eyebrow: 'Capacity', label: 'Context Window', detail: 'Selected context của invocation', role: 'constraint', emphasis: true, size: 'standard'},
    {id: 'llm', eyebrow: 'Decision', label: 'LLM → Reason / Act', detail: 'Answer hoặc tool/action', role: 'model', size: 'standard'},
    {id: 'observe', eyebrow: 'Inspect', label: 'Observe', detail: 'Outcome · freshness · usage · failures', role: 'process', size: 'standard'},
    {id: 'working-state', eyebrow: 'Preserve', label: 'Update Working State', detail: 'Goal · findings · decisions · next actions', role: 'state', size: 'wide'},
    {id: 'evict', eyebrow: 'Refresh', label: 'Evict / Re-retrieve', detail: 'Bỏ stale/duplicate; chuẩn bị next decision', role: 'process', size: 'wide'},
  ]}
  edges={[
    {source: 'sources', target: 'retrieve'},
    {source: 'retrieve', target: 'candidates'},
    {source: 'candidates', target: 'select'},
    {source: 'select', target: 'optimize'},
    {source: 'optimize', target: 'assemble'},
    {source: 'assemble', target: 'window'},
    {source: 'window', target: 'llm'},
    {source: 'llm', target: 'observe'},
    {source: 'observe', target: 'working-state'},
    {source: 'working-state', target: 'evict'},
  ]}
  caption="Canonical flow: candidates được tìm và chọn theo current decision, optimized/assembled trước invocation, rồi outcome cập nhật Working State để context được refresh cho decision tiếp theo."
/>

**Feedback loop:** sau `Evict / Re-retrieve`, agent quay lại `Sources / Retrieve` cho **next decision**. Loop được giải thích bằng prose thay vì thêm một return edge dài dễ làm diagram rối; teaching job chính của diagram là primary lifecycle.

## Four Layers

### 1. Foundations & Lifecycle

**Question:** model cần information nào và nó đến từ đâu?

- Context ≠ Context Window.
- Prompt chỉ là một phần của context.
- Sources tạo khả năng; Retrieve tạo candidates; Select tiêu context capacity.
- Selection phải dựa trên current decision.
- Useful cho toàn task không có nghĩa simultaneous trong một invocation.
- Working State giữ goal/constraints/verified findings qua các invocation.

→ [Context Engineering: Foundations & Lifecycle](./context-engineering.mdx)

### 2. Context Optimization

**Question:** selected context đã đủ tốt và được đóng gói hiệu quả chưa?

- `Good Context = Relevant + Sufficient + Low Noise + Low Redundancy + Fresh + Correct`.
- Budget là ceiling, không phải quota.
- Compression giữ meaning cần thiết với representation cost thấp hơn.
- Assembly tách authority, task, state và evidence rõ ràng.
- Raw evidence phải phân biệt với model interpretation.

→ [Context Optimization](./context-optimization.mdx)

### 3. Context Operations

**Question:** làm sao biết context lifecycle đang hoạt động đúng và an toàn?

- Trace retrieval/selection/budget/freshness/outcome thay vì chỉ nhìn final answer.
- Metrics phải gắn với stage và task outcome, không tối ưu vanity score.
- Freshness/version giúp phát hiện stale evidence.
- Provenance + trust boundaries ngăn untrusted context trở thành high-authority instruction.
- Observability phải cân bằng privacy và redaction.

→ [Context Operations](./context-operations.mdx)

### 4. Practical Context Manager

**Question:** application/agent layer thực thi các quyết định trên thế nào?

- Context Item + metadata/signals.
- pinned / working / evictable context.
- selection, budget và compact Working State.
- raw evidence vs interpretation.
- eviction và lifecycle qua nhiều model invocation.
- minimal Context Manager v0 + controlled experiment vẫn là applied evidence chưa hoàn thành.

→ [Practical Context Manager](./practical-context-manager.mdx)

## The Distinctions to Remember

1. **Context ≠ Context Window** — information khác capacity.
2. **Context Manager ≠ Context Window** — application decision logic khác model capacity.
3. **Retrieve ≠ Select** — retrieval tạo options; selection quyết định thứ đáng chiếm capacity.
4. **Select ≠ Budget ≠ Compress** — chọn cái gì; phân bao nhiêu capacity; biểu diễn với ít token hơn.
5. **Evidence ≠ Interpretation** — raw fact khác hypothesis/summary.
6. **Useful ≠ Simultaneous** — useful cho task không đồng nghĩa phải tồn tại cùng lúc.

## Diagram Review Decision

Sau khi restructure, chỉ giữ visual khi nó có teaching job riêng:

- **Canonical lifecycle:** React Flow ở trang này vì directed topology là bài học chính.
- **Context Window mental model:** Excalidraw trong Foundations vì analogy “cái bàn” giúp intuition.
- **Optimization/Operations:** giữ bằng prose, comparison và examples; không lặp lại lifecycle bằng Pipeline/React Flow khác.
- Không dùng ASCII/Mermaid/Pipeline-card để vẽ lại cùng flow ở nhiều article.

## Completion Boundary

Conceptual knowledge đã được hệ thống hóa nhưng việc restructure **không phải learning evidence mới**. `learning-progress.yaml` vẫn giữ status/confidence hiện tại. Applied evidence còn thiếu:

- implement minimal Context Manager v0;
- controlled raw-context vs managed-context experiment;
- final review + self-test trước khi cân nhắc nâng progress.
