---
title: Evaluation Regression Gates & Production Workflow
description: Biến evaluation evidence thành release decision và dùng production failures để làm eval suite mạnh dần theo thời gian.
tags: [evaluation, regression, production, coding-agent]
---

import {Principle, TLDR} from '@site/src/components/Learning';
import {FlowExplainer} from '@site/src/components/VisualExplainer';

# Evaluation Regression Gates & Production Workflow

## TL;DR

<TLDR>
Evaluation không kết thúc ở một score. Trong production, kết quả evaluation phải đi qua **Regression Gates** để quyết định ship hay block; sau deploy, production failures phải quay lại thành regression cases để Eval Suite ngày càng bao phủ failure modes thực tế.
</TLDR>

<Principle label="Mental rule">
Evaluation đo chất lượng. Regression Gate biến evidence thành release decision. Production failure làm Eval Suite tiến hóa.
</Principle>

## Mental Model

<FlowExplainer
  title="Regression Gate quyết định release và học từ production"
  description="Chọn outcome để theo dõi một candidate qua evaluation evidence, policy gate và feedback từ production."
  stepDurationMs={2600}
  scenarios={[
    {
      id: 'gate-fail',
      label: 'Gate FAIL',
      layout: 'compact',
      presentation: 'progressive',
      nodes: [
        {id: 'candidate', label: 'Candidate', detail: 'Model · prompt · agent · code', role: 'client'},
        {id: 'eval', label: 'Eval Suite', detail: 'Representative + regression cases', role: 'service'},
        {id: 'evidence', label: 'Metrics + Slices', detail: 'Quality · safety · cost · latency', role: 'database'},
        {id: 'gate', label: 'Regression Gate', detail: 'Release policy', role: 'network'},
        {id: 'block', label: 'Block', detail: 'Candidate không được release', role: 'service'},
      ],
      edges: [
        {id: 'fail-candidate-eval', source: 'candidate', target: 'eval'},
        {id: 'fail-eval-evidence', source: 'eval', target: 'evidence'},
        {id: 'fail-evidence-gate', source: 'evidence', target: 'gate'},
        {id: 'fail-gate-block', source: 'gate', target: 'block'},
      ],
      steps: [
        {title: '1 · Evaluate candidate', edgeId: 'fail-candidate-eval', from: 'candidate', to: 'eval', message: 'run evals', description: 'Candidate chạy trên representative cases và những regression cases đã biết.'},
        {title: '2 · Produce decision evidence', edgeId: 'fail-eval-evidence', from: 'eval', to: 'evidence', message: 'results', description: 'Đọc overall metrics cùng critical slices, safety, cost và latency thay vì chỉ một aggregate score.'},
        {title: '3 · Apply release policy', edgeId: 'fail-evidence-gate', from: 'evidence', to: 'gate', message: 'check gates', description: 'Regression Gate áp các threshold và hard constraints lên evaluation evidence.'},
        {title: '4 · Block on required failure', edgeId: 'fail-gate-block', from: 'gate', to: 'block', message: 'FAIL', description: 'Chỉ một required hard gate fail cũng đủ block candidate; metric tốt khác không được bù.'},
      ],
    },
    {
      id: 'gate-pass',
      label: 'Gate PASS',
      layout: 'compact',
      presentation: 'progressive',
      nodes: [
        {id: 'candidate', label: 'Candidate', detail: 'Model · prompt · agent · code', role: 'client'},
        {id: 'eval', label: 'Eval Suite', detail: 'Representative + regression cases', role: 'service'},
        {id: 'evidence', label: 'Metrics + Slices', detail: 'Quality · safety · cost · latency', role: 'database'},
        {id: 'gate', label: 'Regression Gate', detail: 'Release policy', role: 'network'},
        {id: 'deploy', label: 'Deploy', detail: 'Required gates passed', role: 'service'},
        {id: 'production', label: 'Production', detail: 'Observe real outcomes', role: 'database'},
      ],
      edges: [
        {id: 'pass-candidate-eval', source: 'candidate', target: 'eval'},
        {id: 'pass-eval-evidence', source: 'eval', target: 'evidence'},
        {id: 'pass-evidence-gate', source: 'evidence', target: 'gate'},
        {id: 'pass-gate-deploy', source: 'gate', target: 'deploy'},
        {id: 'pass-deploy-production', source: 'deploy', target: 'production'},
      ],
      steps: [
        {title: '1 · Evaluate candidate', edgeId: 'pass-candidate-eval', from: 'candidate', to: 'eval', message: 'run evals', description: 'Candidate chạy trên suite hiện tại để tạo comparable evidence.'},
        {title: '2 · Read evidence by dimension', edgeId: 'pass-eval-evidence', from: 'eval', to: 'evidence', message: 'results', description: 'Overall score không thay thế critical-slice, safety, cost và latency evidence.'},
        {title: '3 · Apply release policy', edgeId: 'pass-evidence-gate', from: 'evidence', to: 'gate', message: 'check gates', description: 'Gate chuyển measurements thành một release decision có policy rõ ràng.'},
        {title: '4 · Deploy only after PASS', edgeId: 'pass-gate-deploy', from: 'gate', to: 'deploy', message: 'PASS', description: 'Candidate chỉ được ship khi mọi required gate đều pass.'},
        {title: '5 · Observe production', edgeId: 'pass-deploy-production', from: 'deploy', to: 'production', message: 'release', description: 'Production cung cấp evidence ngoài coverage của offline Eval Suite.'},
      ],
    },
    {
      id: 'production-feedback',
      label: 'Production feedback',
      layout: 'compact',
      presentation: 'progressive',
      nodes: [
        {id: 'production', label: 'Production Failure', detail: 'Blind spot ngoài current coverage', role: 'database'},
        {id: 'regression', label: 'Regression Case', detail: 'Reproduce failure deterministically', role: 'service'},
        {id: 'eval', label: 'Eval Suite', detail: 'Coverage được mở rộng', role: 'service'},
        {id: 'future', label: 'Future Candidate', detail: 'Phải pass known regression', role: 'client'},
      ],
      edges: [
        {id: 'feedback-production-regression', source: 'production', target: 'regression'},
        {id: 'feedback-regression-eval', source: 'regression', target: 'eval'},
        {id: 'feedback-eval-future', source: 'eval', target: 'future'},
      ],
      steps: [
        {title: '1 · Detect a blind spot', edgeId: 'feedback-production-regression', from: 'production', to: 'regression', message: 'failure found', description: 'Production cho thấy một failure mode mà offline Eval Suite hiện tại chưa bắt được.'},
        {title: '2 · Strengthen the suite', edgeId: 'feedback-regression-eval', from: 'regression', to: 'eval', message: 'add to suite', description: 'Failure được reproduce thành regression case hoặc một representative slice rồi thêm vào Eval Suite.'},
        {title: '3 · Protect future releases', edgeId: 'feedback-eval-future', from: 'eval', to: 'future', message: 'known regression', description: 'Future candidates phải chứng minh failure đã biết không tái diễn trước khi được release.'},
      ],
    },
  ]}
/>

## Core Concepts

### Regression Gate là release policy

Một candidate có overall score tốt hơn chưa đủ để release. Regression Gate mã hóa các constraints thực tế thành policy: quality không regression quá mức cho phép, critical slices đạt threshold, safety violations bằng 0 khi đó là hard requirement, và cost/latency nằm trong production budget.

### Hard gate không được bù bằng metric khác

Nếu policy yêu cầu `PII violations == 0`, một candidate có quality cao hơn, rẻ hơn và nhanh hơn vẫn phải bị block khi có PII violation. Đây là non-compensatory decision: improvement ở metric khác không xóa được failure của critical constraint.

### Critical slices cần được bảo vệ riêng

Aggregate score có thể che failure nghiêm trọng trong một nhóm nhỏ. Security, destructive operations, backward compatibility hoặc những high-risk slices khác có thể cần threshold hay hard gate riêng thay vì chỉ đóng góp vào weighted overall score.

### Production failures phải quay lại Eval Suite

Khi production tìm thấy failure mà Eval Suite không bắt được, đó là một blind spot. Sau khi reproduce được failure, thêm nó thành regression case; nếu incident cho thấy một failure mode rộng hơn, xây một slice gồm nhiều representative cases. Future candidates phải chứng minh rằng failure đã biết không quay lại.

## Example

Agent A đang production. Candidate B có overall success `91%`, security slice `96%`, cost `$0.08/request`, p99 latency `3.5s`, nhưng có `1` PII violation. Policy yêu cầu security `>= 95%`, cost `<= $0.12`, p99 `<= 5s` và `PII violations == 0`.

B phải bị **block** dù các metric còn lại đều pass, vì PII là hard gate.

Sau khi B được sửa và deploy, production phát hiện agent đôi khi xóa một backward-compatible API vẫn được service cũ sử dụng khi refactor module lớn. Team không chỉ fix agent; incident cần được reproduce và đưa vào Eval Suite thành regression case để future changes không lặp lại failure đó.

## When to Use

- Khi cần biến offline evaluation thành release decision cho model, prompt, RAG hoặc agent change.
- Khi có safety, security, compatibility hoặc business constraints không được phép bị aggregate score che khuất.
- Khi production incidents cần trở thành durable evidence cho những lần release sau.

## Common Mistakes

- **Release vì overall score tăng:** aggregate improvement không override critical hard-gate failure.
- **Dùng weighted score cho mọi constraint:** critical safety/security constraints thường cần non-compensatory gate.
- **Fix production bug nhưng không update eval:** cùng failure mode có thể quay lại ở candidate tương lai.
- **Đóng băng Eval Suite:** production distribution và failure modes thay đổi; suite cần tiến hóa từ evidence thực tế.

## Related Knowledge

- [Evaluation Fundamentals](./evaluation-fundamentals.mdx) — mental model từ task success tới production decision.
- [Evaluation Dataset & Test Cases](./evaluation-dataset-and-test-cases.mdx) — thiết kế cases, slices và regression datasets.
- [Evaluation Metrics & Aggregation](./evaluation-metrics-and-aggregation.mdx) — aggregate, slice metrics và hard gates.
- [Evaluation Reliability & Statistical Confidence](./evaluation-reliability-and-statistical-confidence.mdx) — kiểm tra evidence có đủ đáng tin để ra quyết định hay chưa.

## Self-test

1. Regression Gate khác evaluation metric ở đâu?
2. Vì sao candidate tốt hơn về overall quality vẫn có thể bị block?
3. Production failure nên thay đổi Eval Suite như thế nào?
4. Khi nào nên tạo cả regression slice thay vì chỉ một regression case?

<details>
<summary>Answers</summary>

1. Metric đo/tóm tắt kết quả; Regression Gate là policy dùng evidence đó để quyết định release hay block.
2. Vì candidate có thể fail một hard constraint như PII, security hoặc compatibility mà metric khác không được phép bù.
3. Reproduce failure và thêm nó thành regression case để future candidates phải chứng minh failure không quay lại.
4. Khi incident đại diện cho một failure mode rộng hơn cần nhiều representative cases để đo coverage đáng tin cậy.

</details>
